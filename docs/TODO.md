# TODOs

- **Zod bound-violation retry for scoping `generateStructured` calls.** When scoping `generateStructured` calls (e.g. framework generation, where `<max_topics>` / `<min_units>` are enforced post-parse) fail schema validation, do one automated retry feeding the validation error back to the model as corrective context. Currently the transport-level `LLM.maxRetries` covers network/transport only; schema-drift retries are a separate concern. Scoped to scoping for now; generalise to all `generateStructured` callers if/when teaching-session schemas hit the same failure mode. Subsumed by spec §9.2's retry policy — implementation lives in the next-milestone harness loop.

- **Tier-reduction thresholds in `tuning.ts`.** Define when sustained low-quality answers should drop the learner a tier; currently advancement is one-way.

- **Mechanical-MC quality-score mapping confirmed in `tuning.ts`.** Lock the integer-to-SM-2-quality mapping for server-graded multiple-choice answers (correct/incorrect → 5/2 today, but the boundary cases need spec-level sign-off).

- **`llm_call_logs` audit table (post-MVP).** Per-call row capturing prompt hash, token usage, latency, model id — for prompt-cache hit-rate tracking and regression diagnosis.

- **`<curriculum_note>` + `curriculum_notes` table (post-MVP).** When framework editing earns its keep, design the tag schema, persistence table, and consumer (auto-edit vs user-surfaced suggestion) together.

- **`tier_changes` history (post-MVP).** Audit trail of tier advancements/reductions per course (timestamp, from/to tier, trigger). Useful for UI history and progression diagnostics.

- **Auth wiring milestone (its own spec).** Replace dev-user stub with real Supabase Auth + RLS. Spec covers session handling, RLS policies on every table, and migration of dev rows.

- **Cache hot-path verification against Cerebras / OpenAI-compatible.** Confirm prompt-cache hit-rates on production providers; if Cerebras's cache window or hash semantics diverge from OpenAI's, the byte-stable prefix invariant may need adjustment.

- **`turns_remaining` exact attachment point.** Decide whether `<turns_remaining>` rides as its own `harness_turn_counter` row prepended to each user turn, or is inlined into the user message — affects render and replay semantics.

- **Wave turn-parity invariant.** The Wave's final turn (`turns_remaining == 0`) must land on a model response — that turn carries the wrap-up, `<next_lesson_blueprint>`, and `<course_summary_update>`. Without an explicit invariant, an off-by-one or odd/even mismatch in the user↔model ping-pong can let `turns_remaining` reach 0 on a user turn, stranding the next-Wave seed. Decide whether to enforce parity by counting math (e.g. `WAVE.turnCount` always counts model responses, not full exchanges), by harness-side natural-language handling (force one extra turn until the model has emitted the closer), or both. Lock this before the harness loop is built; surface it in spec §6 alongside the `<turns_remaining>` contract.

- **`getNextTurnIndex` read-then-insert race (post-MVP).** The current implementation is safe under the single-user-per-Wave harness loop — concurrent writers cannot occur. If parallel write paths are ever added (e.g. sub-agent harness, multi-device replay), wrap the SELECT MAX + INSERT in a SERIALIZABLE transaction or convert to a per-parent Postgres sequence. See `src/db/queries/contextMessages.ts` for the inline note.

- **`recordAssessment` monotonic-turn race (post-MVP).** Same shape as the `getNextTurnIndex` race above: the SELECT MAX + INSERT in `src/db/queries/assessments.ts` is safe under the single-user-per-Wave invariant, but two concurrent writers could both observe the same `currentMax` and break monotonicity. Hardening (SERIALIZABLE transaction or `pg_advisory_xact_lock` keyed by `waveId`) is deferred until parallel write paths exist; flagged by CodeRabbit on PR #8 and acknowledged inline.

- **File-size cleanup pass (post-merge).** `src/db/queries/courses.ts`, `src/db/migrations/schema.integration.test.ts`, and `src/db/queries/contextMessages.integration.test.ts` exceed the 200-LOC guideline. Splits were intentionally deferred from PR #8 to keep the review-fix diff focused. Suggested splits: courses.ts → courses-reads.ts + courses-writes.ts + courses-utils.ts; integration tests → one file per table.

- **Optional `closed_at >= opened_at` invariant (post-MVP).** Considered for `scoping_passes` and `waves` during Phase B but deferred — adds a CHECK that uses two columns and would have a nontrivial cost on bulk insert. Add when we have a concrete bug or audit requirement that would benefit.

- **JSONB scoping-schema casing inconsistency (post-MVP).** `clarificationJsonbSchema`, `frameworkJsonbSchema`, and `baselineJsonbSchema` in `src/lib/types/jsonb.ts` use snake_case keys (`scope_summary`, `estimated_starting_tier`, `example_concepts`, `baseline_scope_tiers`), while newer JSONB schemas in the same file (`dueConceptsSnapshotSchema`, `seedSourceSchema`) and the prompt-side schemas in `src/lib/prompts/` use camelCase. The divergence was preserved during the scoping-routers feature (D1 decision, plan `docs/superpowers/plans/2026-05-11-scoping-routers.md`): option B was chosen — keep storage shapes as-is and use per-stage `to*Jsonb(...)` projection helpers in `src/lib/course/` to translate from prompt-schema output → JSONB. Translators are unavoidable regardless because JSONB stores aggregated records (LLM emissions + learner answers + grader output) that the LLM cannot produce in one shot, so option B contains the wart in one place rather than rippling case changes through `courses.ts` read guards, `waves.ts`, and ~3 integration-test fixture files. Cleanup belongs in a dedicated PR: rename snake_case fields in the three schemas, update row guards and fixtures, run a one-shot migration to rewrite existing JSONB payloads on `courses.clarification` / `courses.framework` / `courses.baseline`.

- **"Turn" terminology — RESOLVED 2026-07-16, do not re-litigate.** Decision: keep "Turn" = one full learner↔model exchange (diverges from dialogue-systems usage, deliberately). A full industry-vocab rename was audited and rejected — the cost concentrates in immutable DB constraints (`turn_index`, `turn_budget`, `harness_turn_counter`), the model-facing `<turns_remaining>` tag (smoke-verified prompt bytes), and the `data-turn-*` wire part names. Instead, the internal collision was fixed surgically: the chat-scroll type `Turn` → `ChatEntry` (`src/lib/types/chatEntry.ts`), `deriveTurns`/`deriveWaveTurns` → `deriveChatEntries`/`deriveWaveChatEntries`, and `docs/UBIQUITOUS_LANGUAGE.md` now carries a Turn/Step/ChatEntry disambiguation block. New code: never use "turn" for a single message (ChatEntry) or a single API call (Step).

- **Verify streaming renders incrementally (Phase 5 DevTools).** Ollie observed (2026-07-14, Vercel preview) that wave prose appears as one block, not token-by-token. Likely benign — Cerebras generates ~2k tok/s so short turns arrive in one flush — but confirm with AI SDK DevTools / network inspection once Phase 5 observability lands, and check no server-side buffering (e.g. missing flush) is hiding real streaming.

- **`harness_retry_directive` kind — production naming review.** After a few weeks of production logs, revisit the kind name. If "retry directive" causes confusion in prompt-tuning sessions, rename to something like `harness_correction_request` or fold into a single `harness_message` kind with a subtype column.

- **`submitBaseline` `initialSummary` gap.** `setCourseStartingState` requires an `initialSummary` field, but the baseline evaluation schema (`baselineEvaluationSchema` in `src/lib/prompts/baselineEvaluation.ts`) has no summary field. Resolve in the `submitBaseline` spec — likely by adding `<course_summary>` to the evaluator's structured output.

- **Migrate `gradeBaseline` to `executeTurn`, then delete legacy `build*Prompt` builders.** `src/lib/course/gradeBaseline.ts` is the last consumer of `buildBaselinePrompt` (via `buildBaselineEvaluationPrompt` in `src/lib/prompts/baselineEvaluation.ts`). Once migrated, the builders `buildClarificationPrompt`, `buildFrameworkPrompt`, `buildBaselinePrompt`, and their assistant-message / turn-user-content helpers in `src/lib/prompts/{clarification,framework,baseline}.ts` can be deleted (along with their tests); update `src/lib/prompts/index.ts` barrel, and tighten `src/lib/prompts/CLAUDE.md` to drop the "retained for gradeBaseline" paragraph. Pair this with implementing the `submitBaseline` router so the migration has a real consumer.

- **`NotFoundError → NOT_FOUND` tRPC error formatter.** `src/db/queries/errors.ts` `NotFoundError` is a plain `Error`, which tRPC maps to `INTERNAL_SERVER_ERROR`. Add a tRPC error formatter (in `src/server/trpc.ts`) that recognises `NotFoundError` and maps it to `{ code: "NOT_FOUND" }`. Then update the cross-user assertion in `src/server/routers/course.integration.test.ts` from message-text matching to `expect(...).rejects.toMatchObject({ code: "NOT_FOUND" })`. Surfaced in Task 14 code review.

- **Un-park: strict-mode constrained decoding is not enforced — schema-conformance retry churn.** Cerebras receives `response_format: { type: json_schema, strict: true }` for `gpt-oss-120b` but does not enforce it (see auto-memory `cerebras-strict-mode-not-enforced`; verified via the `DESC-FOLLOWING` probe in `scripts/probe-model.ts`). Live UI testing 2026-05-21 showed `failed_assistant_response` on nearly every complex turn — the model emits wrong field names/shapes (`assistantMessage` for `userMessage`, `nextLesson` for `nextUnitBlueprint`, `questionnaire.{type,prompt}` for `questionnaire.questions[]`, `{name,status}` for the `conceptUpdates` shape). `executeTurn`'s validate-and-retry recovers every time, so nothing is user-visibly broken, but each failure costs a retry plus a full uncached re-send. Worst on the wave-close schema (`makeWaveCloseSchema` — gradings + conceptUpdates + nextUnitBlueprint + summary), which has failed first-try on every close turn observed (scoping close and wave close). Options to weigh: (a) find why strict mode degrades — likely a missing `additionalProperties:false` somewhere in the generated schema (Cerebras begins erroring on non-conforming schemas 2026-07-21; before then it appears to silently fall back to unconstrained JSON); (b) simplify the close schema; (c) sharpen the close prompt / `OUTPUT_FORMAT_BLOCK` to name the exact top-level fields. Start with (a) — it's a pure-function check on `toCerebrasJsonSchema`'s output, no LLM call needed.
