# Status — UI QA fixes (round 2): ready to execute

**Date:** 2026-05-22 11:06
**Branch:** `feat/ui-fixes-reference-merge`
**Worktree:** `/Users/olliegilbey/code/nalu/.claude/worktrees/ui-fixes-reference-merge`

## Task Overview

Fix 8 UI bugs found during manual QA of the parent UI batch. Brainstorming and
planning are **done** — spec and implementation plan are written and committed.
The next session is the **coordinating agent for subagent-driven execution**:
dispatch a fresh subagent per plan task, review between tasks, verify green
locally.

Success: all 9 plan tasks complete, `just check` + `just build` green, working
tree clean. PR creation is a **separate, user-authorised step** — do not open a
PR while executing the plan.

## Reference Docs

- **`docs/superpowers/plans/2026-05-22-ui-qa-fixes.md`** — THE plan. 9 tasks,
  every step has complete code. This is what the subagents execute. Self-contained.
- `docs/superpowers/specs/2026-05-22-ui-qa-fixes-design.md` — design rationale
  per issue. Read only if a subagent needs the "why" behind a fix.
- @docs/status/2026-05-22-1029-ui-qa-fixes.md — prior handoff. Holds the
  **verbatim 8 user-reported issues** and the parent-batch "Important
  Discoveries" (it @ mentions the 2226 file). Reference for the discoveries
  list; the RCA in it is now superseded by the spec.

## Current State

**Committed** (on top of `main`):

- `80fe385 docs: spec for UI QA fixes round 2`
- `6e6a4bd docs: implementation plan for UI QA fixes round 2`

**Uncommitted working tree** (intentional — these are plan **Task 1**, not loose
ends):

- `src/components/chat/Splash.tsx` — issues 1/2/3 (copy), applied last session.
- `src/components/chat/ChatShell.tsx` — issue 7 (scroll bob), applied last session.
- `.claude/settings.json` — unrelated plugin swap; user explicitly wants it
  committed too (Task 1 Step 4).
- `docs/status/` untracked — these handoffs; committed in plan **Task 9**.

GH issues filed (backend follow-ups, out of scope): **#15** (merge
generateFramework+generateBaseline into one LLM call), **#16** (scoping-flow
retry path). Already linked in the spec.

## Important Discoveries

**Decisions locked during brainstorming** (do not re-litigate):

- Issues 1/2/3/7 are folded into this batch (committed in Task 1).
- Issues 6+8 optimistic bubble: gate on a `turns.length` snapshot, NOT
  `isPending`. One fix kills both the duplicate (6) and the vanish-on-error (8).
- On a submit error the questionnaire stays **dismissed** from the composer —
  the composer reverts to its free-text input; answers already show as a bubble.
- localStorage restore bug IS in scope (plan Tasks 3+4).

**Execution constraints — these bite subagents specifically:**

- **Subagents MUST use ABSOLUTE paths.** A relative path resolves against the
  main repo, not this worktree. (Parent-batch Discovery 4.)
- **Worktree is deliberate.** Another agent is active in the main repo on
  `feat/anonymous-auth`. Do not touch the main repo or that worktree.
- **1Password commit-signing locks after inactivity.** A commit step may need a
  Touch ID tap; if signing fails, that's the cause — re-tap, do not bypass.
- **Never** `--no-verify` / bypass hooks. Pre-commit runs on every commit (fast,
  ~2s); CI re-checks everything anyway.
- `just build` must run from the worktree (Turbopack `root` pin — Discovery 1).
- No chat-component unit-test harness (Discovery 7). Components verified by
  `just typecheck && just lint && just test`. Pure helpers are TDD'd.
- `just check` runs `test-int` (Postgres testcontainer, needs Docker). If Docker
  is unavailable, fall back to `just lint && just typecheck && just test` and
  say integration tests were skipped. The pre-commit hook itself is light.

**Verified against current code** (RCA is sound, plan reflects it): Composer's
3-effect ordering bug, the `isPending`-gated optimistic bubble, the 3 scoping
mutations with no `onError`, the framework→baseline auto-dispatch double-call.

## Next Steps

1. Start subagent-driven execution of `docs/superpowers/plans/2026-05-22-ui-qa-fixes.md`.
   Use the `superpowers:subagent-driven-development` skill: fresh subagent per
   task, two-stage review between tasks.
2. Tasks run in order 1→9. Task dependencies: Task 4 needs Task 3's helper;
   Tasks 5+6 need Task 2's helper. Each task ends with its own commit.
3. After Task 8, a manual `just dev` walkthrough is recommended (not a gate —
   needs Touch ID for the 1Password-injected env).
4. Task 9 is the full-check gate + commits the status docs.
5. Then stop — PR is a separate user-authorised step.

VERBATIM — where work stopped (user choosing execution mode, then asking for
this wrap-up):

> "Subagter-driven, but let's do a /wrap-up first so that we store the best
> context for the coordinating agent to become the parent of the subagents with
> all the useful information and references that will help, and can throw out
> the useless context."

(User typo "Subagter-driven" = subagent-driven.)

## Context to Preserve

- **Subagent model:** Opus 4.7 for implementer and reviewer subagents.
- **Subagent method:** implementers use red→green TDD; the controller verifies
  green **locally**, never defers to CI.
- **Communication:** extremely concise; sacrifice grammar for brevity.
- **Conventional commits, lowercase subject.** Exact messages are in the plan.
- **Ask before:** git commits beyond the plan's, PRs, destructive ops. The
  plan's per-task commits are pre-authorised by the user approving the plan.
- The user dislikes em-dashes in user-facing copy (not in code/docs).
- Simplification bias: Nalu trends toward overcomplication — if a subagent finds
  a simpler collapse mid-task, surface it rather than gold-plating.
- Promise made: GH issues #15/#16 filed (done). Spec + plan committed (done).

## Restart Hint

Clean to resume — spec + plan committed, working tree changes are plan Task 1
(not loss). Safe to /clear. Next session: read this file + the plan, then begin
subagent-driven execution at Task 1.
