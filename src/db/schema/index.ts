/**
 * Schema barrel — stub only.
 *
 * Task B8 will replace this with real table exports. This empty export
 * exists so that `import * as schema from "@/db/schema"` in the A6 harness
 * files typechecks without pulling in any schema shape prematurely.
 */
export {};
