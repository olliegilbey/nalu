import { TopicInput } from "@/components/chat/TopicInput";

/** Root route — renders the empty-state topic composer that initiates scoping. */
export default function Home() {
  return <TopicInput />;
}
